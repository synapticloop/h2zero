package synapticloop.sample.h2zero.sqlite3.finder;

// - - - - thoughtfully generated by synapticloop h2zero - - - - 
//    with the use of synapticloop templar templating language
//                (java-create-finder.templar)

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.math.BigDecimal;
import java.util.List;
import java.util.ArrayList;

import synapticloop.h2zero.base.exception.H2ZeroFinderException;
import synapticloop.h2zero.base.manager.sqlite3.ConnectionManager;
import synapticloop.h2zero.util.LruCache;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


import synapticloop.sample.h2zero.sqlite3.model.util.Constants;

import synapticloop.sample.h2zero.sqlite3.model.AuthorStatus;

public class AuthorStatusFinder {
	// the binder is unused in code, but will generate compile problems if this 
	// class is no longer referenced in the h2zero file. Just a nicety for
	// removing dead code
	@SuppressWarnings("unused")
	private static final String BINDER = Constants.AUTHOR_STATUS_BINDER;

	private static final Logger LOGGER = LoggerFactory.getLogger(AuthorStatusFinder.class);
	private static final String SQL_SELECT_START = "select id_author_status, txt_author_status, txt_desc_author_status from author_status";
	private static final String SQL_BUILTIN_FIND_BY_PRIMARY_KEY = SQL_SELECT_START + " where id_author_status = ?";


	// now for the statement limit cache(s)
	private static LruCache<String, String> findAll_limit_statement_cache = new LruCache<String, String>(1024);

	private AuthorStatusFinder() {}

	/**
	 * Find a AuthorStatus by its primary key
	 * 
	 * @param connection the connection item
	 * @param idAuthorStatus the primary key
	 * 
	 * @return the unique result or throw an exception if one couldn't be found
	 * 
	 * @throws H2ZeroFinderException if one couldn't be found
	 */
	public static AuthorStatus findByPrimaryKey(Connection connection, Long idAuthorStatus) throws H2ZeroFinderException {
		AuthorStatus authorStatus = null;
		PreparedStatement preparedStatement = null;
		ResultSet resultSet = null;

		if(null == idAuthorStatus) {
			throw new H2ZeroFinderException("Could not find result as the primary key field [idAuthorStatus] was null.");
		}

		try {
			preparedStatement = connection.prepareStatement(SQL_BUILTIN_FIND_BY_PRIMARY_KEY);
			preparedStatement.setLong(1, idAuthorStatus);
			resultSet = preparedStatement.executeQuery();
			authorStatus = uniqueResult(resultSet);
		} catch (SQLException sqlex) {
			throw new H2ZeroFinderException(sqlex);
		} catch (H2ZeroFinderException h2zfex) {
			throw new H2ZeroFinderException(h2zfex.getMessage() + "  Additionally, the parameters were [idAuthorStatus:" + idAuthorStatus + "].");
		} finally {
			ConnectionManager.closeAll(resultSet, preparedStatement);
		}

		if(null == authorStatus) {
			throw new H2ZeroFinderException("Could not find result the parameters were [idAuthorStatus:" + idAuthorStatus + "].");
		}
		return(authorStatus);
	}

	/**
	 * Find a AuthorStatus by its primary key
	 * 
	 * @param idAuthorStatus the primary key
	 * 
	 * @return the unique result or throw an exception if one couldn't be found.
	 * 
	 * @throws H2ZeroFinderException if one couldn't be found
	 */
	public static AuthorStatus findByPrimaryKey(Long idAuthorStatus) throws H2ZeroFinderException {

		if(null == idAuthorStatus) {
			throw new H2ZeroFinderException("Could not find result as the primary key field [idAuthorStatus] was null.");
		}

		AuthorStatus authorStatus = null;
		try (Connection connection = ConnectionManager.getConnection()) {
			authorStatus = findByPrimaryKey(connection, idAuthorStatus);
		} catch (SQLException | H2ZeroFinderException ex) {
			throw new H2ZeroFinderException(ex.getMessage() + "  Additionally, the parameters were [idAuthorStatus:" + idAuthorStatus + "].");
		}

		if(null == authorStatus) {
			throw new H2ZeroFinderException("Could not find result the parameters were [idAuthorStatus:" + idAuthorStatus + "].");
		}
		return(authorStatus);
	}

	/**
	 * Find a AuthorStatus by its primary key and silently fail.
	 * I.e. Do not throw an exception on error.
	 * 
	 * @param connection the connection item
	 * @param idAuthorStatus the primary key
	 * 
	 * @return the unique result or null if it couldn't be found
	 * 
	 */
	public static AuthorStatus findByPrimaryKeySilent(Connection connection, Long idAuthorStatus) {
		try {
			return(findByPrimaryKey(connection, idAuthorStatus));
		} catch(H2ZeroFinderException h2zfex){
			if(LOGGER.isWarnEnabled()) {
				LOGGER.warn("H2ZeroFinderException findByPrimaryKeySilent(" + idAuthorStatus + "): " + h2zfex.getMessage());
				if(LOGGER.isDebugEnabled()) {
					h2zfex.printStackTrace();
				}
			}
			return(null);
		}
	}

	/**
	 * Find a AuthorStatus by its primary key and silently fail.
	 * I.e. Do not throw an exception on error.
	 * 
	 * @param idAuthorStatus the primary key
	 * 
	 * @return the unique result or null if it couldn't be found
	 * 
	 */
	public static AuthorStatus findByPrimaryKeySilent(Long idAuthorStatus) {
		try {
			return(findByPrimaryKey(idAuthorStatus));
		} catch(H2ZeroFinderException h2zfex){
			if(LOGGER.isWarnEnabled()) {
				LOGGER.warn("H2ZeroFinderException findByPrimaryKeySilent(" + idAuthorStatus + "): " + h2zfex.getMessage());
				if(LOGGER.isDebugEnabled()) {
					h2zfex.printStackTrace();
				}
			}
			return(null);
		}
	}

	/**
	 * Find all UserTitle objects with the passed in connection, with limited
	 * results starting at a particular offset.
	 * 
	 * If the limit parameter is null, there will be no limit applied.
	 * 
	 * If the offset is null, then this will be set to 0
	 * 
	 * If both limit and offset are null, then no limit and no offset will be applied
	 * to the statement.
	 * 
	 * The passed in connection object is usable for transactional SQL statements,
	 * where the connection has already had a transaction started on it.
	 * 
	 * If the connection object is null an new connection object will be created 
	 * and closed at the end of the method.
	 * 
	 * If the connection object is not null, then it will not be closed.
	 * 
	 * @param connection - the connection object to use (or null if not part of a transaction)
	 * @param limit - the limit for the result set
	 * @param offset - the offset for the start of the results.
	 * 
	 * @return a list of all of the UserTitle objects
	 * 
	 * @throws SQLException if there was an error in the SQL statement
	 */
	public static List<AuthorStatus> findAll(Connection connection, Integer limit, Integer offset) throws SQLException {
		boolean hasConnection = (null != connection);
		String statement = null;
		// first find the statement that we want

		String cacheKey = limit + ":" + offset;
		if(!findAll_limit_statement_cache.containsKey(cacheKey)) {
			// place the cacheKey in the cache for later use

			StringBuilder stringBuilder = new StringBuilder(SQL_SELECT_START);

			if(null != limit) {
				stringBuilder.append(" limit ");
				stringBuilder.append(limit);
				if(null != offset) {
					stringBuilder.append(" offset ");
					stringBuilder.append(offset);
				}
			}


			statement = stringBuilder.toString();
			findAll_limit_statement_cache.put(cacheKey, statement);
		} else {
			statement = findAll_limit_statement_cache.get(cacheKey);
		}

		// now set up the statement
		PreparedStatement preparedStatement = null;
		ResultSet resultSet = null;
		if(connection == null) {
			connection = ConnectionManager.getConnection();
		}

		List<AuthorStatus> results = new ArrayList<AuthorStatus>();

		try {
			preparedStatement = connection.prepareStatement(statement);
			resultSet = preparedStatement.executeQuery();
			results = list(resultSet);
		} catch(SQLException sqlex) {
			if(LOGGER.isWarnEnabled()) {
				LOGGER.warn("SQLException findAll(): " + sqlex.getMessage());
				if(LOGGER.isDebugEnabled()) {
					sqlex.printStackTrace();
				}
			}
			throw sqlex;
		} finally {
			if(hasConnection) {
				ConnectionManager.closeAll(resultSet, preparedStatement, null);
			} else {
				ConnectionManager.closeAll(resultSet, preparedStatement, connection);
			}
		}

		return(results);
	}

	public static List<AuthorStatus> findAll() throws SQLException {
		return(findAll(null, null, null));
	}

	public static List<AuthorStatus> findAll(Connection connection) throws SQLException {
		return(findAll(connection, null, null));
	}

	public static List<AuthorStatus> findAll(Integer limit, Integer offset) throws SQLException {
		return(findAll(null, limit, offset));
	}

	public static List<AuthorStatus> findAllSilent(Connection connection, Integer limit, Integer offset) {
		try {
			return(findAll(connection, limit, offset));
		} catch(SQLException sqlex){
			if(LOGGER.isWarnEnabled()) {
				LOGGER.warn("SQLException findAllSilent(connection: " + connection + ", limit: " +  limit + ", offset: " + offset + "): " + sqlex.getMessage());
				if(LOGGER.isDebugEnabled()) {
					sqlex.printStackTrace();
				}
			}
			return(new ArrayList<AuthorStatus>());
		}
	}

	public static List<AuthorStatus> findAllSilent(Connection connection) {
		return(findAllSilent(connection, null, null));
	}

	public static List<AuthorStatus> findAllSilent(Integer limit, Integer offset) {
		return(findAllSilent(null, limit, offset));
	}

	public static List<AuthorStatus> findAllSilent() {
		return(findAllSilent(null, null, null));
	}

	/* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
	 * 
	 * This is the start of the user defined finders which are generated
	 * through either the "finders" JSON key, or the "fieldFinders" JSON
	 * key.
	 * 
	 * There are 0 defined finders on the author_status table:
	 * 
	 * 
	 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

	/**
	 * Return a unique result for the query - in effect just the first result of
	 * query.  If there is a second result (i.e. the query did not return the 
	 * expected unique result), then an exception will be thrown.
	 * 
	 * @param resultSet The result set of the query
	 * 
	 * @return The AuthorStatus that represents this result
	 * 
	 * @throws H2ZeroFinderException if no results were found or more than one result was found
	 * @throws SQLException if there was a problem retrieving the results
	 */
	private static AuthorStatus uniqueResult(ResultSet resultSet) throws H2ZeroFinderException, SQLException {
		if(resultSet.next()) {
			// we have a result
			Long idAuthorStatus = ConnectionManager.getNullableResultLong(resultSet, 1);
			String txtAuthorStatus = ConnectionManager.getNullableResultString(resultSet, 2);
			String txtDescAuthorStatus = ConnectionManager.getNullableResultString(resultSet, 3);

			AuthorStatus authorStatus = new AuthorStatus(idAuthorStatus, txtAuthorStatus, txtDescAuthorStatus);

			if(resultSet.next()) {
				throw new H2ZeroFinderException("More than one result in resultset for unique finder.");
			} else {
				return(authorStatus);
			}
		} else {
			// could not get a result
			return(null);
		}
	}

	/**
	 * Return the results as a list of AuthorStatus, this will be empty if
	 * none are found.
	 * 
	 * @param resultSet the results as a list of AuthorStatus
	 * 
	 * @return the list of results
	 * 
	 * @throws SQLException if there was a problem retrieving the results
	 */
	private static List<AuthorStatus> list(ResultSet resultSet) throws SQLException {
		List<AuthorStatus> arrayList = new ArrayList<AuthorStatus>();
		while(resultSet.next()) {
			arrayList.add(new AuthorStatus(
					ConnectionManager.getNullableResultLong(resultSet, 1),
					ConnectionManager.getNullableResultString(resultSet, 2),
					ConnectionManager.getNullableResultString(resultSet, 3)));
		}
		return(arrayList);
	}

	/* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
	 * 
	 * This is the start of the user defined select clause finders which are 
	 * generated through the "finders" JSON key, with a 'selectClause' 
	 * key on the finder.
	 * 
	 * All selectClause finders return a subset of the data from a row of the 
	 * database table (or tables if there is a join statement) as a generated
	 * bean
	 * 
	 * There are 0 defined finders on the author_status table:
	 * 
	 * 
	 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * */

}