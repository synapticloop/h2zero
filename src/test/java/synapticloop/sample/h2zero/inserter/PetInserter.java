package synapticloop.sample.h2zero.inserter;

// - - - - thoughtfully generated by synapticloop h2zero - - - - 
//    with the use of synapticloop templar templating language
//                (java-create-inserter.templar)

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.sql.Date;

import synapticloop.h2zero.base.manager.sqlite3.ConnectionManager;
import synapticloop.sample.h2zero.model.util.Constants;

public class PetInserter {
	// the binder is unused in code, but will generate compile problems if this 
	// class is no longer referenced in the h2zero file. Just a nicety for
	// removing dead code
	@SuppressWarnings("unused")
	private static final String BINDER = Constants.PET_BINDER;

	// static fields generated by synapticloop h2zero
	private static final String SQL_BUILTIN_INSERT_ALL = "insert into pet(id_pet, nm_pet, num_age, flt_weight, dt_birthday)";
	private static final String SQL_BUILTIN_INSERT_VALUES = SQL_BUILTIN_INSERT_ALL + " values (?, ?, ?, ?, ?)";
	// static inserter SQL generated from the user input

	private PetInserter() {}

	public static int insert(Connection connection, Long idPet, Boolean nmPet, Integer numAge, Float fltWeight, Date dtBirthday) throws SQLException {
		int numResults = -1;
		PreparedStatement preparedStatement = null;
		try {
			preparedStatement = connection.prepareStatement(SQL_BUILTIN_INSERT_VALUES);
			ConnectionManager.setBigint(preparedStatement, 1, idPet);
			ConnectionManager.setBoolean(preparedStatement, 2, nmPet);
			ConnectionManager.setInt(preparedStatement, 3, numAge);
			ConnectionManager.setFloat(preparedStatement, 4, fltWeight);
			ConnectionManager.setDate(preparedStatement, 5, dtBirthday);
			numResults = preparedStatement.executeUpdate();
		} catch (SQLException sqlex) {
			throw sqlex;
		} finally {
			ConnectionManager.closeAll(preparedStatement);
		}
		return(numResults);
	}

	public static int insert(Long idPet, Boolean nmPet, Integer numAge, Float fltWeight, Date dtBirthday) throws SQLException {
		int numResults = -1;
		Connection connection = null;
		try {
			connection = ConnectionManager.getConnection();
			numResults = insert(connection, idPet, nmPet, numAge, fltWeight, dtBirthday);
		} catch (SQLException sqlex) {
			throw sqlex;
		} finally {
			ConnectionManager.closeAll(connection);
		}
		return(numResults);
	}

	public static int insertSilent(Connection connection, Long idPet, Boolean nmPet, Integer numAge, Float fltWeight, Date dtBirthday) {
		int numResults = -1;
		try {
			numResults = insert(connection, idPet, nmPet, numAge, fltWeight, dtBirthday);
		} catch (SQLException sqlex) {
			sqlex.printStackTrace();
		}
		return(numResults);
	}

	public static int insertSilent(Long idPet, Boolean nmPet, Integer numAge, Float fltWeight, Date dtBirthday) {
		int numResults = -1;
		Connection connection = null;
		try {
			connection = ConnectionManager.getConnection();
			numResults = insert(connection, idPet, nmPet, numAge, fltWeight, dtBirthday);
		} catch (SQLException sqlex) {
			sqlex.printStackTrace();
		} finally {
			ConnectionManager.closeAll(connection);
		}
		return(numResults);
	}

}