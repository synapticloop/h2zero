package com.synapticloop.book.h2zero.sqlite3.inserter;

// - - - - thoughtfully generated by synapticloop h2zero - - - - 
//    with the use of synapticloop templar templating language
//            (java/java-create-inserter.templar)

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.math.BigDecimal;
import java.sql.Timestamp;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.synapticloop.h2zero.base.manager.sqlite3.ConnectionManager;
import com.synapticloop.book.h2zero.sqlite3.model.util.Constants;

/**
 * This class contains the methods to insert new rows into the: 
 *   <code>book.user</code>
 * table.  The fields that are available are as follows:
 * 
 * <ul>
 *  <li><code>id_user</code> (bigint)  <strong>NOT</strong> nullable  (PRIMARY)</li>
 *  <li><code>id_user_type</code> (bigint)  <strong>NOT</strong> nullable </li>
 *  <li><code>nm_user</code> (varchar)  <strong>NOT</strong> nullable </li>
 *  <li><code>num_age</code> (int)  <strong>NOT</strong> nullable </li>
 *  <li><code>fl_is_active</code> (boolean)  nullable </li>
 *  <li><code>dtm_signup</code> (datetime)  nullable </li>
 * </ul>
 * 
 * @author synapticloop h2zero
 */

public class UserInserter {
	// the binder is unused in code, but will generate compile problems if this 
	// class is no longer referenced in the h2zero file. Just a nicety for
	// removing dead code
	@SuppressWarnings("unused")
	private static final String BINDER = Constants.USER_BINDER;

		private static final Logger LOGGER = LoggerFactory.getLogger(UserInserter.class);

	// static fields generated by synapticloop h2zero
	private static final String SQL_BUILTIN_INSERT_ALL = "insert into user(id_user, id_user_type, nm_user, num_age, fl_is_active, dtm_signup)";
	private static final String SQL_BUILTIN_INSERT_VALUES = SQL_BUILTIN_INSERT_ALL + " values (?, ?, ?, ?, ?, ?)";
	// static inserter SQL generated from the user input

	private UserInserter() {}

	/**
	 * Insert a new User into the database utilising the passed in connection.
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * @param flIsActive  maps to fl_is_active
	 * @param dtmSignup  maps to dtm_signup
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Connection connection, Long idUser, Long idUserType, String nmUser, Integer numAge, Boolean flIsActive, Timestamp dtmSignup) throws SQLException {
		try (PreparedStatement preparedStatement = connection.prepareStatement(SQL_BUILTIN_INSERT_VALUES)) {
			ConnectionManager.setBigint(preparedStatement, 1, idUser);
			ConnectionManager.setBigint(preparedStatement, 2, idUserType);
			ConnectionManager.setVarchar(preparedStatement, 3, nmUser);
			ConnectionManager.setInt(preparedStatement, 4, numAge);
			ConnectionManager.setBoolean(preparedStatement, 5, flIsActive);
			ConnectionManager.setDatetime(preparedStatement, 6, dtmSignup);
			return(preparedStatement.executeUpdate());
		}
	}

	/**
	 * Insert a new User into the database utilising the passed in connection 
	 * with only the fields that are allowed to be not null.
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Connection connection, Long idUser, Long idUserType, String nmUser, Integer numAge) throws SQLException {
		try (PreparedStatement preparedStatement = connection.prepareStatement(SQL_BUILTIN_INSERT_VALUES)) {
			ConnectionManager.setBigint(preparedStatement, 1, idUser);
			ConnectionManager.setBigint(preparedStatement, 2, idUserType);
			ConnectionManager.setVarchar(preparedStatement, 3, nmUser);
			ConnectionManager.setInt(preparedStatement, 4, numAge);
			ConnectionManager.setBoolean(preparedStatement, 5, null);
			ConnectionManager.setDatetime(preparedStatement, 6, null);
			return(preparedStatement.executeUpdate());
		}
	}

	/**
	 * Insert a new User into the database a new connection will be retrieved 
	 * from the pool, used and then closed.
	 * 
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * @param flIsActive  maps to fl_is_active
	 * @param dtmSignup  maps to dtm_signup
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Long idUser, Long idUserType, String nmUser, Integer numAge, Boolean flIsActive, Timestamp dtmSignup) throws SQLException {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idUser, idUserType, nmUser, numAge, flIsActive, dtmSignup));
		}
	}

	/**
	 * Insert a new User into the database a new connection will be retrieved 
	 * from the pool, used and then closed. This is for fields which have a nullable allowed default
	 * 
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Long idUser, Long idUserType, String nmUser, Integer numAge) throws SQLException {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idUser, idUserType, nmUser, numAge));
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new User into the 
	 * database utilising the passed in connection. If an exception is thrown by the
	 * method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * @param flIsActive  maps to fl_is_active
	 * @param dtmSignup  maps to dtm_signup
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Connection connection, Long idUser, Long idUserType, String nmUser, Integer numAge, Boolean flIsActive, Timestamp dtmSignup) {
		try {
			return(insert(connection, idUser, idUserType, nmUser, numAge, flIsActive, dtmSignup));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new User into the 
	 * database utilising the passed in connection. If an exception is thrown by the
	 * method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * This is only for Non-Nullable fields
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Connection connection, Long idUser, Long idUserType, String nmUser, Integer numAge) {
		try {
			return(insert(connection, idUser, idUserType, nmUser, numAge));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new User into the 
	 * database, creating and closing a connection in the process. If an exception is thrown 
	 * by the method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * @param flIsActive  maps to fl_is_active
	 * @param dtmSignup  maps to dtm_signup
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Long idUser, Long idUserType, String nmUser, Integer numAge, Boolean flIsActive, Timestamp dtmSignup) {
		try (Connection connection = ConnectionManager.getConnection()){
			return(insert(connection, idUser, idUserType, nmUser, numAge, flIsActive, dtmSignup));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new User into the 
	 * database, creating and closing a connection in the process. If an exception is thrown 
	 * by the method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * This is for non-nullable fields only
	 * 
	 * @param idUser  maps to id_user
	 * @param idUserType  maps to id_user_type
	 * @param nmUser  maps to nm_user
	 * @param numAge  maps to num_age
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Long idUser, Long idUserType, String nmUser, Integer numAge) {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idUser, idUserType, nmUser, numAge));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

}