package synapticloop.workitemiser.database.mysql.h2zero.inserter;

//        - - - - thoughtfully generated by synapticloop h2zero - - - -        
//          with the use of synapticloop templar templating language
//                     (/java/inserter/java-create-inserter.templar)

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.math.BigDecimal;
import java.sql.Date;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.synapticloop.h2zero.base.manager.mysql.ConnectionManager;
import synapticloop.workitemiser.database.mysql.h2zero.model.util.Constants;

/**
 * <p>This class contains the methods to insert new rows into the: 
 *   <code>invoicer.agency_agreement</code>
 * table.  The fields that are available are as follows:</p>
 * 
 * <ul>
 *  <li><code>id_agency_agreement</code> (bigint)  <strong>NOT</strong> nullable  (PRIMARY)</li>
 *  <li><code>num_strata_plan</code> (int)  <strong>NOT</strong> nullable </li>
 *  <li><code>dt_agency_agreement_start</code> (date)  <strong>NOT</strong> nullable </li>
 *  <li><code>dt_agency_agreement_end</code> (date)  <strong>NOT</strong> nullable </li>
 *  <li><code>flt_percent_increase</code> (float)  nullable </li>
 *  <li><code>txt_notes</code> (varchar)  nullable </li>
 *  <li><code>fl_is_current</code> (boolean)  <strong>NOT</strong> nullable </li>
 * </ul>
 * 
 * @author synapticloop h2zero
 */

public class AgencyAgreementInserter {
	// the binder is unused in code, but will generate compile problems if this 
	// class is no longer referenced in the h2zero file. Just a nicety for
	// removing dead code
	@SuppressWarnings("unused")
	private static final String BINDER = Constants.AGENCY_AGREEMENT_BINDER;

		/**
	 * <p>The default logger implementation</p>
	 */
	private static final Logger LOGGER = LoggerFactory.getLogger(AgencyAgreementInserter.class);

	// static fields generated by synapticloop h2zero
	private static final String SQL_BUILTIN_INSERT_ALL = "insert into agency_agreement(id_agency_agreement, num_strata_plan, dt_agency_agreement_start, dt_agency_agreement_end, flt_percent_increase, txt_notes, fl_is_current)";
	private static final String SQL_BUILTIN_INSERT_VALUES = SQL_BUILTIN_INSERT_ALL + " values (?, ?, ?, ?, ?, ?, ?)";
	// static inserter SQL generated from the user input

	private AgencyAgreementInserter() {}

	/**
	 * <p>Insert a new <code>AgencyAgreement</code> into the database utilising the passed in connection.</p>
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param fltPercentIncrease  maps to flt_percent_increase
	 * @param txtNotes  maps to txt_notes
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Connection connection, Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Float fltPercentIncrease, String txtNotes, Boolean flIsCurrent) throws SQLException {
		try (PreparedStatement preparedStatement = connection.prepareStatement(SQL_BUILTIN_INSERT_VALUES)) {
			ConnectionManager.setBigint(preparedStatement, 1, idAgencyAgreement);
			ConnectionManager.setInt(preparedStatement, 2, numStrataPlan);
			ConnectionManager.setDate(preparedStatement, 3, dtAgencyAgreementStart);
			ConnectionManager.setDate(preparedStatement, 4, dtAgencyAgreementEnd);
			ConnectionManager.setFloat(preparedStatement, 5, fltPercentIncrease);
			ConnectionManager.setVarchar(preparedStatement, 6, txtNotes);
			ConnectionManager.setBoolean(preparedStatement, 7, flIsCurrent);
			return(preparedStatement.executeUpdate());
		}
	}

	/**
	 * <p>Insert a new AgencyAgreement into the database utilising the passed in connection 
	 * with only the fields that are allowed to be not null.</p>
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Connection connection, Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Boolean flIsCurrent) throws SQLException {
		try (PreparedStatement preparedStatement = connection.prepareStatement(SQL_BUILTIN_INSERT_VALUES)) {
			ConnectionManager.setBigint(preparedStatement, 1, idAgencyAgreement);
			ConnectionManager.setInt(preparedStatement, 2, numStrataPlan);
			ConnectionManager.setDate(preparedStatement, 3, dtAgencyAgreementStart);
			ConnectionManager.setDate(preparedStatement, 4, dtAgencyAgreementEnd);
			ConnectionManager.setFloat(preparedStatement, 5, null);
			ConnectionManager.setVarchar(preparedStatement, 6, null);
			ConnectionManager.setBoolean(preparedStatement, 7, flIsCurrent);
			return(preparedStatement.executeUpdate());
		}
	}

	/**
	 * <p>Insert a new AgencyAgreement into the database a new connection will be retrieved 
	 * from the pool, used and then closed.</p>
	 * 
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param fltPercentIncrease  maps to flt_percent_increase
	 * @param txtNotes  maps to txt_notes
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Float fltPercentIncrease, String txtNotes, Boolean flIsCurrent) throws SQLException {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, fltPercentIncrease, txtNotes, flIsCurrent));
		}
	}

	/**
	 * <p>Insert a new AgencyAgreement into the database a new connection will be retrieved 
	 * from the pool, used and then closed. This is for fields which have a nullable allowed default.</p>
	 * 
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 * 
	 * @throws SQLException if there was an error in the SQL insert statement
	 */
	public static int insert(Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Boolean flIsCurrent) throws SQLException {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, flIsCurrent));
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new AgencyAgreement into the 
	 * database utilising the passed in connection. If an exception is thrown by the
	 * method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param fltPercentIncrease  maps to flt_percent_increase
	 * @param txtNotes  maps to txt_notes
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Connection connection, Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Float fltPercentIncrease, String txtNotes, Boolean flIsCurrent) {
		try {
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, fltPercentIncrease, txtNotes, flIsCurrent));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new AgencyAgreement into the 
	 * database utilising the passed in connection. If an exception is thrown by the
	 * method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * This is only for Non-Nullable fields
	 * 
	 * @param connection the connection to use for the database, this __MUST__ be 
	 *   closed by the calling function.
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Connection connection, Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Boolean flIsCurrent) {
		try {
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, flIsCurrent));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new AgencyAgreement into the 
	 * database, creating and closing a connection in the process. If an exception is thrown 
	 * by the method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param fltPercentIncrease  maps to flt_percent_increase
	 * @param txtNotes  maps to txt_notes
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Float fltPercentIncrease, String txtNotes, Boolean flIsCurrent) {
		try (Connection connection = ConnectionManager.getConnection()){
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, fltPercentIncrease, txtNotes, flIsCurrent));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

	/**
	 * Silently (i.e. swallow any exceptions) Insert a new AgencyAgreement into the 
	 * database, creating and closing a connection in the process. If an exception is thrown 
	 * by the method, the exception message will be logged as an 'error', if 'trace' logging
	 * is enabled, the stack trace will be printed to the output stream.
	 * 
	 * This is for non-nullable fields only
	 * 
	 * @param idAgencyAgreement  maps to id_agency_agreement
	 * @param numStrataPlan  maps to num_strata_plan
	 * @param dtAgencyAgreementStart  maps to dt_agency_agreement_start
	 * @param dtAgencyAgreementEnd  maps to dt_agency_agreement_end
	 * @param flIsCurrent  maps to fl_is_current
	 * 
	 * @return the number of rows that were inserted, or -1 if an error occurred
	 */
	public static int insertSilent(Long idAgencyAgreement, Integer numStrataPlan, Date dtAgencyAgreementStart, Date dtAgencyAgreementEnd, Boolean flIsCurrent) {
		try (Connection connection = ConnectionManager.getConnection()) {
			return(insert(connection, idAgencyAgreement, numStrataPlan, dtAgencyAgreementStart, dtAgencyAgreementEnd, flIsCurrent));
		} catch (SQLException sqlex) {
			LOGGER.error("SQLException caught, message was: {}", sqlex.getMessage());
			if(LOGGER.isTraceEnabled()){
				sqlex.printStackTrace();
			}
			return(-1);
		}
	}

}