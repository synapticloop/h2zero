<?xml version="1.0" encoding="UTF-8"?>
<project name="common-build" default="help">
	<dirname property="common.basedir" file="${ant.file.common-build}" />

	<!--
	  Print out a help message
	  -->
	<target name="help" description="[common-build] Print out a help message with a list of available tasks">
		<echo>type:</echo>
		<echo>  ant -projecthelp</echo>
		<echo>or</echo>
		<echo>  ant -p</echo>
		<echo>for a complete task list</echo>
		<echo></echo>
		<echo>Note:</echo>
		<echo>  This only prints out those target elements that have a description attribute</echo>
	</target>

</project>
